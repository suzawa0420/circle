# 2026-09-25 検索・お問い合わせの取得処理改善

## 対象

基準コミット: a0f49d5313815ded5656b11ad26d6064ca0b5e3c。
ユーザー承認: タグ検索・キーワード検索と関連遅延要因の修正、日本語コミット、masterへのpush、本番デプロイ。

- TagsController: タグ・市区町村の所属から各Userを取得するN+1を廃止。user_idサブクエリで絞り込む。
- Circles::SearchController: キーワードごとに取得していた6種類のID配列をサブクエリに変更。
- 検索結果の有無の確認は全件数取得でなくexists?を使用。未指定qは空文字として既存の一覧へ戻す。
- UserContactsController#new: 管理者全件の実体化をIDサブクエリに変更。
- 検索条件、ワイルドカードの意味、並び順、ページサイズ、フォーム送信処理は維持。
- DB migration、インデックス、Unicornワーカー数、ネットワーク、課金プランは変更しない。

## 検証

隔離されたローカルPostgreSQL16の合成データのみを使用。Rails全体や本番DBは起動・接続せず、認証情報は読まない。
`test/performance/search_queries_check.rb` は9 tests / 1165 assertions成功。

- タグの5アクション × 3ソート × 3ページの結果・件数一致。
- キーワード10パターン × 3ソート × 3ページの旧実装との結果一致。
- 複数語、全角空白、英大小文字、部分一致ワイルドカード、引用符、該当なし、重複所属を確認。
- 47件のタグ所属の検証で48 SELECTから1 SELECTへ削減（ID絞り込みの比較）。
- 2語検索の事前ID取得は12 SELECTから0へ。結果取得のSQLは別途必要。
- お問い合わせ候補の順序を維持し、AdminUserの実体化0件。
- 既存の一覧上限付き取得テストも実施。

## リリースと確認

既存GitHub ActionsがServer3、Server2の順に反映・アセット生成・安全なUnicorn再起動・登録フォームreadinessを実行する。
既存のdb:migrateステップは残るが、このリリースに新規migrationはない。
masterはpush直前に再確認し、force pushしない。
公開GETのHTTPステータスと時間を少数回確認する。アカウント作成やお問い合わせPOSTはしない。
本番確認結果はデプロイ後に報告する。

## 限界とロールバック

デプロイ前の公開/circlesのGETで502を一度観測。既存のCPUバースト枯渇があり、応答時間は不安定。
DBの本番実行計画、実リクエストごとのSQL時間、完全な画面E2Eは未検証。N+1削減だけで全遅延解消とは判断しない。
前方ワイルドカード付き部分一致の費用は残る。追加のインデックスや検索基盤は本番実行計画に基づく別作業とする。
新規の継続的5xx・検索結果の欠落・readiness失敗があれば展開状況を確認し、今回のコミットをrevertして既存workflowで再展開する。
元の作業ディレクトリの無関係な未コミット変更は今回に含めず、そのまま保持する。
